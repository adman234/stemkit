# Python engine for the web/Docker build. torch and torchaudio are installed
# first from the PyTorch CUDA index (see Dockerfile), so pip keeps them.
demucs==4.0.1
numpy==1.26.4
beartype==0.22.9
einops==0.8.2
rotary-embedding-torch==0.9.1
# imported by the vendored roformer attention module
packaging
# model configs for msst.py (demucs pulls it in too)
pyyaml
# CQT and beat tracking for chords.py
librosa>=0.10,<1
# unpinned on purpose: YouTube breaks old releases, and the container also
# refreshes these on start (YTDLP_AUTO_UPDATE)
yt-dlp
yt-dlp-ejs
